# Post-observation amendments

## 23 September 2026 — exact five-cycle frontier

The declared numerical five-cycle experiment exposed an additional structure at epsilon=0: maximal average success after one agent withdraws was 7 delta / 4, and the nondominated segment appeared to connect (25 delta / 12, 7 delta / 4) to (5 delta / 2, 3 delta / 2). This is exploratory theory prompted by the results, not a predeclared prediction.

We derived an analytic upper bound by averaging over the dihedral symmetries of the contract cycle. A two-edge profile bounds the two endpoint probabilities in a four-agent path. A separate event containing the two deleted edges and the surviving middle edge bounds compatibility with the full profile. An explicit all-profile mechanism and convex mixtures attain the resulting frontier for 0 < delta <= 2/5. Added a separate exact rational checker of every event, every adjacent profile pair, and 21 mixture weights at three rational delta values. These additional checks are reported separately from the initial 762 / 63 / 288 experiments.

No primary benchmark settings or results were removed or changed.

The epsilon=log(2) LP results also motivated a third explicit mechanism: probabilities delta on the two path-end edges, zero on the path-middle edge, delta/3 per edge at full participation, and the same smaller-profile rules as the epsilon=0 frontier endpoint. Its full-cube privacy proof establishes success 2 delta at every one-withdrawal profile. Three additional rational mechanisms are checked with multiplier 2. Thus loosening epsilon strictly improves optimal ex ante success while leaving every pointwise optimum unchanged. This is an exploratory analytic result, not a retrospective predeclared claim.
